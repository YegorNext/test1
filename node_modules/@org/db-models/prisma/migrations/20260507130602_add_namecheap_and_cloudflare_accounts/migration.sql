-- AlterTable
ALTER TABLE "Server" ALTER COLUMN "expiredAt" SET DEFAULT (now() + interval '1 month' - interval '1 day');

-- CreateTable
CREATE TABLE "NamecheapAccount" (
    "id" TEXT NOT NULL,
    "username" TEXT NOT NULL,
    "apiUser" TEXT NOT NULL,
    "apiKey" TEXT NOT NULL,
    "contacts" JSONB,

    CONSTRAINT "NamecheapAccount_pkey" PRIMARY KEY ("id")
);

-- CreateTable
CREATE TABLE "CloudflareAccount" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "apiToken" TEXT NOT NULL,
    "accountId" TEXT NOT NULL,

    CONSTRAINT "CloudflareAccount_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE UNIQUE INDEX "NamecheapAccount_username_key" ON "NamecheapAccount"("username");

-- CreateIndex
CREATE UNIQUE INDEX "CloudflareAccount_name_key" ON "CloudflareAccount"("name");
