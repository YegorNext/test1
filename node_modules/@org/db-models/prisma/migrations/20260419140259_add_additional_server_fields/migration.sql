-- AlterTable
ALTER TABLE "Server" ADD COLUMN     "assignedAt" TIMESTAMP(3),
ADD COLUMN     "renewalAt" TIMESTAMP(3),
ALTER COLUMN "expiredAt" SET DEFAULT (now() + interval '1 month' - interval '1 day');

-- Backfill:
UPDATE "Server"
SET "assignedAt" = "createdAt"
WHERE "userId" IS NOT NULL;