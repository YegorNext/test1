/*
  Warnings:

  - You are about to drop the column `campaigns` on the `User` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE "Server" ALTER COLUMN "expiredAt" SET DEFAULT (now() + interval '1 month' - interval '1 day');

-- AlterTable
ALTER TABLE "User" DROP COLUMN "campaigns";

-- CreateTable
CREATE TABLE "Campaign" (
    "id" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "alias" TEXT NOT NULL,
    "geo" TEXT[],
    "userId" TEXT NOT NULL,

    CONSTRAINT "Campaign_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE INDEX "Campaign_userId_idx" ON "Campaign"("userId");

-- AddForeignKey
ALTER TABLE "Campaign" ADD CONSTRAINT "Campaign_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE;
